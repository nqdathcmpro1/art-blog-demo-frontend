import { makeStyles } from "@mui/styles";

export default makeStyles({
  container: {
    width: "100%",
    border: "1px solid black",
  },

  tableContainer: {
    width: "100%",
  },

  noArt: {
    textAlign: "center",
    fontStyle: "italic",
    fontWeight: "bold",
  },
});
