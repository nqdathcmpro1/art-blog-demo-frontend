export const artsSelector = (state) => state.artList.arts;

export const searchArtsSelector = (state) => state.artList.searchArts;

export const authorArtsSelector = (state) => state.artList.authorArts;

export const manageArtsSelector = (state) => state.artList.manageArts;

export const currentArtSelector = (state) => state.artList.currentArt;

export const pendingSelector = (state) => state.artList.isPending;
